server:
  port: 8080 #项目启动端口
  tomcat:
    uri-encoding: utf-8
spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  profiles:
    include: jdbc,mybatis,quartz,redis #引入数据库相关配置






